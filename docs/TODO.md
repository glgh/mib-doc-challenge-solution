# TODO: the try-list (2026-07-25 OCR autopsies + cascade co-fire audit)

_The single todo tracker for this work. Update statuses in place; log scored results as experiments.md rows and link them here. Background evidence: BACKGROUND §3 (flag label mining, incl. the mangled-value geometry table), STATUS Q6/Q7. Fact-checked 2026-07-25 against docs, caches, and code; anchor strings verified verbatim in the ensemble caches._

**Working style (user directive): hard examples first.** Every item below names its anchor cases and the expected per-case outcome. An item is tried against its anchors before any full-corpus measurement is spent; the full dump regenerates only when something has already proven itself on the anchors. No "regenerate everything" as step one.

**The anchor set** (dev split except where marked; ladder galleries and page renders from 2026-07-25; similarity ratios throughout are on lowercase-alnum-normalized text — raw `SequenceMatcher` gives different numbers):

| case/page | what it shows | truth |
| --- | --- | --- |
| MIB-000990 p1 | tilted render beat deskewed sibling via well-formed-but-WRONG `MIB-000000` (+1 CASE_ID_RE); TSV conf ranks repaired variants higher; flag value shattered across 14 hard-ensemble reads: `Beghie_ ju. ics` / `Begibie_|.._ics` / `Bagte——I*` / `Hagia;` | `illegible_biometrics` printed — **recovered** (row 36, quorum) |
| MIB-000252 p2 | gray stock, blotchy toner; geometry fine, every rung optical-fails; mangle `Bagitie bematics` (difflib 0.53 — but weighted-sim 0.64, see BACKGROUND §3) | printed — **recovered** (row 36, single-read) |
| MIB-000595 p3 | shred bands stay offset after deshred (border itself shredded); `Mogible_ biometrics` → 0.83 either metric | printed — **recovered** (row 36, single-read) |
| MIB-000656 / 979 | token legible verbatim, but page's OCR header defeats `detect_doc_type` → flag scan used to skip the page | printed — **recovered** (row 35, gate deleted) |
| MIB-000771 | same doc-gate miss; token one glyph off (`Begible_biometrics`) — still resolved by `match_flag_token` once the page is scanned | printed — **recovered** (row 35) |
| MIB-000747 p5, MIB-000506 | `Observed flags: [RISK PANEL MISSING]` / `[RISK PANEL = NG]` read perfectly — damage marker, no token | **controls: must NOT emit** |
| MIB-000096 p2 | dead page: 8 variants all debris, no doc type; field block is a faint smudge | condition-derived flag — **control only** (Track 4: condition emission is dead) |
| MIB-000135 | no B-13 anywhere, all-text packet | condition-derived — **control only** |
| MIB-000065 p3 | text rotated 90° and cropped mid-field at page edge | info genuinely absent — **control only** |
| 025 date / 037 name | valid-vs-valid variant-vote ties where generation order picks wrong (STATUS Q7 / merge-probe `681bbee`) | **HOLDOUT — do not tune on these.** 1.4 must re-mine dev-split ties first |

Instruments already built: `output/cache/reads_hard.jsonl` (92-case hard ensemble, optical rungs on), `output/cache/train_bands.jsonl` (full-train ensemble, schema 2 with `struck` backfilled — supersedes `train_bands_reads.jsonl` as the current substrate), `experiments/mine_hard.py`, `scripts/replay.py` (frozen-behavior reference gate), `experiments/ladder_gallery.py` (promoted from scratchpad 2026-07-25), `experiments/crop.py` (high-zoom page-region crops — REQUIRED before reading/judging any page image, model and human alike; contact sheets compress a print line to a few pixels, user directive 2026-07-26).

---

## Track 1 — OCR confidence (Q7)

### 1.1 ☑ TSV conf probe — GRADUATED (user-judged galleries, 2026-07-25)
`experiments/conf_probe.py`, 13 pages (3 anchors + 10 stratified hard picks). Findings:
- **The `ev` pathology does not transfer**: 990's hallucinated `MIB-000000` line got conf 36 (page junk level); every repaired variant out-confs the tilted render `ev` crowned.
- **Metric shape matters**: plain mean is gamed by rotated few-confident-words reads; raw mass by debris volume; render-source boilerplate (footer `Packet MIB-… / page N`, watermark — conf 90+) biases any mass metric toward `render`. Winner: **guarded excess mass** = Σ max(0, conf−40) per word over non-page-furniture lines. User verdict on the 8 differ-pages: conf-pick no worse.
- **TSV line text ≠ stdout text on 93/111 reads** — "same pass, free conf" is false; 1.2 must run TSV additively (2nd pass) or re-baseline.
- Incidental: the `Packet MIB-XXXXXX / page N` footer premise of 3.3 is real on probe pages.

### 1.2 ☑ Conf captured in the pipeline — SHIPPED (rows 41 + 44)
Single invocation `txt tsv` (text byte-identical); `Read.conf` per-line tuples; schema 3 (row 41), then schema 4 added the tsv line TEXT as a 4th element (row 44, `52c12eb`) making per-line conf queryable. Subset workflow live: `output/cache/hard_conf4.jsonl` (82 hard-set cases) + `hard_conf4_opt.jsonl` (optical) are the iteration substrate; `verify_render` compares at the cache's conf arity and honors the cache's `select` stamp.

### 1.3 ☑ Conf selection SHIPPED as default (rows 41–43, dev 123.02 → 123.17, CFA 0)
Guarded excess mass (`records.conf_excess_mass`, baseline 40, positional footer guard) is the selector; `MIB_SELECT=ev` was deleted in the de-special-casing batch (2026-07-26 — Phase E done). A/B: the 990 anchor flipped off the tilted render; 4 better / 4 worse fields, all four regressions dispatched — 523 by the identity_conflict variant-agreement clause (row 43), 708/1000 wait on per-line conf ties, 293 on per-field conf. Sensitivity: baseline 40–50 plateau, 30 loses. 1.5 died unbuilt. Confidence-table refit deferred to the next substrate change.

### 1.4 ☑ Vote overhaul: merge-don't-tie + per-line conf tie-break — SHIPPED (rows 45 + 47)
Row 45 (`612c615`): vote keys strip edge punctuation per token and (names only) merge stroke collapses `rn`≡`m` any-position / `ri`≡`n` token-final, rep = expanded form (census-bound; inner `ri` merge conflated `Miradane`/`Miradarie` and was position-restricted out). Genuine ties break by `_line_conf` (alnum-substring into the schema-4 tsv text), then first-seen. 6 better / 0 worse full-train. Row 47 (`f4c48ba`): inner-colon junk filter. REJECTED in the same batch: truncation pooling (consolidation strengthens whichever family fragments more — 250's decoy bloc outvoted truth; needs a per-page two-level vote) and intake-over-sponsor name preference (census says the opposite: sponsor-text right 28:5, sponsor-OCR right 12:4, but 14/16 conflicts already resolve). **Open residue**: ~9 vote-reachable name anchors (truncation ties 258/340/506/990, wrong-plurality 262/299/389/536) wait on per-line conf over the FULL corpus (schema-4 regen) — the 3-3 family ties these reduce to have no arbiter in a schema-3 cache. Prior page-level weighting + revoked-tie guard reverts stand (row 43).

### 1.5 ☑ `evidence_score` pathology fix — DIED via 1.3 as expected
`evidence_score` itself was deleted with the ladder enumerator (de-special-casing batch, 2026-07-26); conf selection has no shape-validity credit to game.

### 1.6 ☑ GOOD_ENOUGH audit — score deleted; the residue is WEAK_BAR
`evidence_score` and `GOOD_ENOUGH` are gone (de-special-casing batch); the surviving hand-picked bar is `render.WEAK_BAR=6` on the frozen `page_score` (optical/last-resort gate, and `mine_hard.py`'s hard cut). The 1.6 debt transfers to it: re-derive from the dev score distribution when the gate matters (the ev valley sat at 5, not 6 — the demonstrated sparse-receipt artifact in 5.2 was exactly this bar).

### 1.7 ◐ Arbitration metrics — page-selection grain CLOSED (row 53); the A1/A2 metrics survive, re-aimed (user call 2026-07-26)
The three mis-ranking modes (023 NL-prior under-payment, 352 volume bias, 794 watermark mass) are real, and the candidate fixes DO repair the metric: the min-words + watermark guards flip the 085-class debris payments and structurally zero out turned-geometry reads; the domain-weighted floor pays letter-perfect `AQUARIAN MANTIS` what the LSTM's language prior refuses to. What died is one consumer at one grain: `experiments/probe_arbitration.py` replayed all seven variants over the full corpus and they score IDENTICALLY on dev (954 page-winner flips → 2 holdout garble-for-garble changes), because the S4 pooled vote sits downstream of page-level primary selection and absorbs the choice (row 49 measured the same fact as "page-level conf weight rejected"). Do not reopen *page-level selection* without evidence that something consumes `best_read` un-voted. The metrics themselves (`m_guards`/`m_dom`/`domain_hits` in `probe_arbitration.py`, anchor-verified) are a library awaiting the grains where NO vote absorbs them: 3.4's seam-window acceptance (a raw mass comparison), the `_line_conf` tie-break (the 1.4 name-tie residue — now with a measured 3-case active-mistake list from the row-66 audit: 085 visa, 395 sponsor digit, 609 name; see 5.11), and 1.8's fusion token scoring. Oracle bound from the same lab (`arb_oracle.jsonl`): 57 of 702 wrong dev scalar fields reachable by ANY selection policy — 41 applicant_name; species/world/purpose 0 (per-read snap is saturated).

### 1.8 ☑ Cross-read token fusion — CLOSED (rows 63–66): names shipped, exceedance shipped, residue measured noise-dominated
EXCEEDANCE SHIPPED 2026-07-26 (row 65, `packet.closed_vocab_fallback` + `experiments/exceed_probe.py`): value-first line scanning + cross-read aggregation fills the closed-vocab fields the merge left empty, display-only. Dev FIXED 39 / LOST 0 (species 9, world 13, purpose 17 incl. 11 junk replacements); holdout free observation 13/0. RESIDUE CLOSED by the deep probe (row 66, `exceed_probe.py deep`, floor 0.30 + fragment-half check): of the 60 no-hit targets only 2 are decoder-winnable and 2 fragment-assembly cases; 52 have sub-floor signal that LOSES to debris (the 0.30–0.50 band is noise-dominated — junk piles onto attractor entries like ANDROMEDAN faster than truth accumulates); 4 fully dead. And the low-floor aggregate keeps only 17/40 of the shipped rule's wins, so any char-consensus/decoder build must gate strictly behind the shipped fill — ceiling ~5 fields ≈ +0.04 dev. Same session, same verdict for `visa_class` (the one field row 65 debatably excluded): pattern-vote census over the 41 missing-pred cases finds **39/41 with no trace of any visa pattern in any read** — the fee-silence shape on a regex field, 1 recoverable. **Conclusion: value MATCHING is saturated at every grain measured (per-read snap, line aggregate, sub-floor decode, digit re-arbitration, flag bars); the remaining extraction mass is READ-CREATION — pages where no read holds the value in any form (Track 3 repairs, 6.7's PSM-3 last resort).** Original framing below kept for the record.
NAME PROGRAM CLOSED 2026-07-26: the 23 grid-reachable names decomposed into (i) multi-applicant attribution — SHIPPED as the corroboration challenge, row 63 (≥2 docs AND strictly more than incumbent; FIXED 6/BROKE 0; id-anchoring impossible — the active id is stamped on every page); (ii) junk/truncation vote winners — SHIPPED as the shape guard, row 64 (FIXED 2 incl. 990's truncation class); (iii) the residue (single-doc truths like 064/243/293/885 where corroboration cannot reach and weaker clauses measured net-negative — one-doc + dominant-vote broke 11) is recorded SATURATED on current signals. Digit fields (sponsor/arrival) also saturated: near-miss family re-arbitration nets ±2 of 332 (row 62 preamble). What remains OPEN here is closed-vocab EXCEEDANCE — species 37 / home_world 50 / purpose 53 wrong dev fields, 0 reachable per-read: assembling a snappable string from tokens no single read contains (per-token consensus pre-snap, A1 domain-aware scoring from the 1.7 library). That build was not attempted; it is the last extraction program with a three-digit wrong-count.
GRID REFRESH (2026-07-26, both oracle artifacts regenerated on `train_grid.jsonl` + grid predictions; pre-grid baselines kept as `*_pregrid.jsonl`): 525 wrong dev scalar fields (was 702), 63 reachable (was 57) — applicant_name 23 (was 41; the grid + name-pool snap + closed-vocab rank converted 18), fee_status 20 (was 0 — all `paid→truth-unknown`, SHIPPED as row 61's explicit-unknown tier), visa 7, sponsor 7 (digit-level misreads incl. two revoked-id collisions 578/928), arrival 6 (single-digit/month misreads — per-character consensus territory), species/world/purpose still 0. The original framing below still holds with the new member list (`output/viz/arb_oracle.jsonl`; `--pred` flag added so the oracle always pairs cache with same-rev predictions): selection can reach only the reachable set, and closed-vocab fields are 0-reachable per-read — so fusion's value is (i) the name program: 41 reachable names split into case-normalization near-misses (`ixovara Tekix`), wrong-page attribution (truth name lives in another page's reads — a merge/decoy question, not OCR), and garble-vote losses (the 1.4 residue's 9 anchors); (ii) EXCEEDING the per-read oracle on closed-vocab fields by assembling a snappable string from tokens no single read contains (per-token consensus pre-snap). Line matching across variants (y_frac + fuzzy text) is the substrate for both. Token scoring should use A1's domain-aware conf (1.7 library), not raw conf — the LSTM's language prior under-pays exactly the domain tokens fusion needs to keep (the 023 mode, which would otherwise reappear inside the consensus). `output/viz/arb_oracle.jsonl` is the target list. The B1 regen is DONE (row 55) — no blocking dependencies left.

### 1.9 ◐ Fee residue — (a) explicit-unknown tier SHIPPED (row 61), fuzzy-key repair CLOSED; (b) policy-feed still a USER DECISION
Shipped: `packet.fee_fallback` (display-only ladder, fee 63%→85%, row 54) + `_fee_unknown_stated` (row 61: unpaid > DIP-WAIVER > unknown > paid; four statement shapes fuzzy-detected — inline kv, split-line receipt, prose reason/manual note, damage marker with truncations; dev FIXED 24 / BROKE 10 / lateral 5, all breaks genuine planted `unknown` receipts on truth-paid labels). Fuzzy fee-KEY repair (the old (a), `Fee Stan waved` class) is CLOSED by the row-61 census: for garble-keyed truth-paid lines the base-rate tier already lands paid, and every legible non-unknown receipt value in the fee_unknown branch is a **struck value** — NOT a "planted trap" as row 61 first framed it (user challenge 2026-07-26, verified in the cache: 514's `unpaid` carries `struck=['unpaid']` on a truth-paid case, 614 likewise, 855's `paid` is struck beside a visible DIP-WAIVER on truth-waived). The in-world model is a document lifecycle: the receipt printed an earlier state, the state changed, the old value was crossed out — the row-34 class (452 struck receipts, 0 counterexamples that a struck value is ever the truth). These cases sit in fee_unknown precisely because the strike-void correctly deleted the value; the rule "receipt values must never feed policy nor be fuzz-recovered" stands with a sharper reason: raw-line fuzzy recovery is blind to strike annotations and would resurrect exactly what the pipeline already correctly voided. Residue ~0. UPDATE row 69: at the SNAP grain the fuzz guard was removed on user call (priced −0.01, one lost fee field on 688) — the no-fuzz rule above now applies only to the fallback-ladder tiers (`_fee_unknown_stated` / `fee_fallback`, unchanged); 3.6 raster strike detection is the structural protection going forward. Still open: (b) USER DECISION — feed the base-rate paid tier to policy for +0.22 dev (16 approvals / 9 review losses / 1 CFA — MIB-000332, irreducible; the 44 truth-APPROVED silent fee_unknown reviews are this class's ceiling); (c) audit other autocontrast consumers for the OCR-visible white-text injection channel `_INJECTION_RE` guards in the fee path (114/058 anchors); (d) **verbatim-outranks-reconstructed vote rank** (from 688, row 69): a snap-RECONSTRUCTED value should rank below a VERBATIM read of a competing value in `_variant_vote`'s group rank — 688's 4 reconstructed `unpaid` (all `naid`-class decapitations) outvote 2 verbatim `paid`; verbatim-priority is the general, guard-free defense against systematic-garble fan-out electing a wrong vocab entry. Needs a corpus A/B: count current vote winners that are reconstructed-over-verbatim and truth-mark them before shipping.

### 1.10 ☑ risk_flags oracle — BOUND CLOSED (row 56; `experiments/flags_oracle.py`, detail `output/viz/flags_oracle.jsonl`)
GRID REFRESH (2026-07-26): 123 wrong / 132 missing instances, classes stable (90 unreachable, 31 hidden-only, 7 inferred-only, 3 near-miss, 1 value_blocked, 0 extra) — the row-56 verdict survives the substrate change unchanged. Original bound: 127 wrong dev risk_flags on the live substrate, ALL under-emission (0 extra — the scan never hallucinates). The 138 missing instances decompose: 92 unreachable (no reading holds the flag in any form; Track 3 or genuinely absent), 36 hidden-only (answer-key injections carrying true values — bait, correctly refused), 7 inferred-only (organizer ruling §1 forbids emitting), 2 near-miss, 1 value_blocked. **Track 2 is SATURATED at ≤3 instances (~0.3 raw pts) — do not invest in matchers/bars.** The one live mechanism: `observed_flags`' `if hits: continue` skips the whole-value rescue on a line the token path already resolved a different flag from, so a two-flag value line emits only its legible half (anchor MIB-000414 `Coserved flags: b-chazarc_red, Beg e_biometrics` — second flag reaches the quorum bar on two independent readings). Micro-fix if ever wanted: split the labelled value on commas, rescue residual segments even when the line has a token hit; +1–2 instances max. Flags headroom otherwise belongs to Track 3 (the 92) or is not headroom at all.

## Track 2 — flag-line recovery ("line present, matcher/gate too strict")

### 2.1 ☑ Delete the flag-scan doc-type gate — SHIPPED (row 35, dev 122.44 → 122.79, CFA 0)
Doc-type filter dropped from `observed_flags`; `has_flag_evidence`'s positive clause shares the widened scan; the `'flag' + none/clear` negative clause stays biometric-restricted (CFA-risk direction, unmeasured). Anchors passed (656/771/979 emit, 747/506 silent) — but the "exactly three cases change" expectation **failed in the good direction**: 18 cases changed, every added flag true (the gate was also blocking planetary_embargo/biohazard_red/identity_conflict/ memory_tampering on header-mangled pages; the P=1.00 mining had only measured illegible_biometrics). 3 correct NEEDS_REVIEW→DENIED flips, so classification moved after all. Regression tests from the mined lines + controls; confidence table refit (score-neutral).

### 2.2 ☑ Unified weighted flag-value resolver — SHIPPED (row 36, dev 122.79 → 122.86, CFA 0)
Absorbed old 2.2 *and* 2.3 as one mechanism. `vocab.match_flag_value` (confusion-weighted, alnum-normalized, whole-value); `snap("observed_flags")` refactored onto `match_flag_token` (the parallel per-token difflib@0.8 path is gone — one metric, one threshold vocabulary). Bars mined from the full safety table (`experiments/flag_probe.py --values`, 563 values): every false at ≤0.40/≤0.08, every true at ≥0.44/≥0.10 → single-read 0.55/0.15, quorum-2 at 0.44/0.10, OCR readings only. Anchors exact: 252/595 emit single-read, 990 by quorum, 747/506 / `biometrics ok` / `none` silent; replay diff was exactly the three anchors + holdout 438 (planetary_embargo by quorum-6, truth-confirmed). Regression tests shipped. The mining also surfaced the space-split class (`illegible biometrics` scores 1.00 whole, invisible to the token path). Broken-toner note: repair can't read 252/990 (optical rungs tried live in session — more mangles, no matchable value; *unrecorded* beyond commit `b1199a5`'s dev-wide 11-recovered/10-corrupted A/B), so this resolver was the only live path to that class.

## Track 3 — visual defects

### 3.1 ☐ Color-aware masking (`imaging`, pre-`to_gray`)
Saturation mask → paper white: stamps (252 red, 990 purple — session observation, unrecorded), SAMPLE DENIAL watermark (96/595), yellow highlights (65/252) are colored; evidence ink is black. **Anchors are controls here:** on all four autopsy pages the overlays miss the field lines, so expect no change — the value, if any, is elsewhere in the corpus. Try on the hard set first; before/after gallery to the user (house rule: their eyes judge repair quality).

### 3.2 ◐ Content-anchored deshred — PARTIALLY SHIPPED as the `local` rung (row 51); the destroyed-border class remains
Row 51 shipped `imaging.realign_local`: at seams that cut through text, the cut glyph halves' cross-correlation overrides the border's implied shift (including "the text never moved" — 165's rescue). What it does NOT cover: pages whose border is itself shredded to dashes (the confetti class — 237/243/595: thin one-line bands, ~no text on cuts, 8-17% left-line presence; findings.md 2026-07-26). For those the fix is not repair but standing down (no-op class: zero cuts-text seams + healthy base read, both census-computable) — and 595's flag value, if recoverable at all, needs the seam-window acceptance below. **Anchor:** 595 p3 (gallery to user).

### 3.4 ☐ Seam-window conf acceptance (per-band guard; QT-E licensed)
Accept/reject repair candidates by conf mass in windows around text-cutting seams instead of whole-page mass — deshred's conf gain concentrates there (+39 cuts-text vs +3 whitespace seams) and whole-page conf is not name-safe (037 inversion). The structural fix for 085-class arbitration displacements, and the only guard granular enough to ever ship 037's Case-ID-line fix (whole-page ties always lose). Acceptance metric: the A1/A2 library from 1.7 (`m_guards`/`m_dom` — min-words + watermark guards, domain-weighted floor), NOT the raw shipped mass — window acceptance is a bare metric comparison with no vote downstream to absorb a debris word or watermark inside the window, which is exactly the grain 1.7's fixes were built for (085's +15 junk payment is the anchor). Composes with band-level content votes for mid-band text on whitespace-seam bands (037's actual garble source). New veto case from the row-55 pricing: 578/590, where local-rung churn snapped sponsor misreads to REVOKED ids (SPN-4040, SPN-0139) — a seam-window guard should refuse a repair variant that mutates an id line into a revoked match (id-mutation veto, cheap to check with `domain_hits`).

### 3.6 ☒ Raster strike detection — CLOSED (eye-audit 2026-07-26: zero scan-strike instances corpus-wide)
The structural fact stands — `extract._red_strikes` reads red VECTOR strokes only, so a strike baked into a scan raster would be invisible — but the user's eye-audit dissolved EVERY candidate: **191 and 893 are label errata** (documents print an unstruck `unpaid`, the `paid` labels are wrong — FIELDS.md fee section), **284/355 are note-superseded** (manual corrections supply waived/paid at rank 0 and already win the merge). The strike channel is therefore **text-layer-complete** (row 34's vector detection covers every real instance: 514/614/855 verified struck spans) and there is nothing for a raster detector to find. Do not build. Reopen only if a genuine scan-raster strike ever surfaces (none known on train).

### 3.5 ☐ Stamp-fragment tolerance (S3, from the confetti finding)
243's `COPY` stamp and its `MIB` adjudication box are torn across many thin bands — multi-band line structures no row-realign reassembles, and stamps are evidence precedence rank 1. Check S3 stamp/adjudicator-finding detection for fragmentation tolerance; anchor 243 p2 (crops in the 2026-07-26 session gallery).

### 3.3 ☐ Footer registration anchor (exploratory, only if 1–2 stall)
**Premise unverified:** no doc records a `Packet MIB-XXXXXX / page N` footer at a known position — BACKGROUND §4 names the page *border* as the registration signal. First render a page sample and check the footer exists, survives crop/shred, and sits where claimed; only then design orientation + skew + case-id cross-check around it. Mostly a runtime win if real.

## Track 4 — dispositions (no action unless reopened)

- **Optical rung** (`MIB_OCR_OPTICAL`): REOPENED and flipped ON by default (row 48). Its ev-era killer (well-formed binarized garbage outscoring correct reads, 11 recovered / 10 corrupted unguarded) does not transfer to conf selection: hard-set A/B under conf measured 1 better / 0 worse / 2 neutral, and the one hazard it exposed (optical primary dropping the adjudicator Finding line on 065) was fixed structurally by the row-46 variant fallback. `ocr_optical` promoted to CRITICAL_KEYS (None-tolerant join guard).
- **Condition-based flag emission**: dead. P(truth | any observable condition) ≤0.38; the generator's B-13-included-then-destroyed vs never-included distinction is invisible in the PDF (BACKGROUND §3). Printed evidence only — which is also organizer ruling §1.
- **Docker gate at shipped cost** (STATUS Q3 second half): standing, unrelated to this track. Note the failure fallback changed: the ladder enumerator is deleted (de-special-casing batch), so a grid-cost failure now means reverting that commit from git history, not a one-line `DEFAULT_PLAN` flip.

## Track 5 — cascade structure (2026-07-25 co-fire audit; instrument: `experiments/cofire_probe.py`)

_The audit evaluated all 16 branch predicates independently per dev case (STATUS question 8's order component). Probe stays in sync with `policy.adjudicate` order — it asserts first-match equivalence and reports mismatches._

### 5.1 ☑ Order audit + stale-over-fee reorder — SHIPPED (row 39, dev 122.86 → 123.02, CFA 0)
Order was decision-relevant on exactly one co-fire cell: `fee_unknown` preempting `stale_arrival`. Truth-side cell empty on all 1,000 labels (thin alone — ~1.6 expected under independence; the decision stands on the 7-of-8 truth-DENIED pipeline mix + CFA-safe direction); 8 corpus flips NR→DENIED. The priced cost is MIB-000096 — the dead-page anchor control, truth NR: its arrival read is *correct* (genuinely stale); the OCR ensemble vote fabricated visa `MED-3` against a truth of DIP-1, so `known_non_dip` armed on wrong-but-positive evidence and the case lost its staleness exemption. Pre-reorder it was right by accident via `fee_unknown`. Confidence refit + regression test (`test_stale_arrival_outranks_an_unknown_fee`). Open follow-up: the §6 drift check for this reorder is 5.6; the 096 failure class is 5.8.

### 5.2a ☑/☐ Fee-pool diagnosis (2026-07-26, rows 50 + census; the fee half of the conservative-review mass)
Measured on the schema-4 substrate: of the fee-unknown/truth-payable pool, the readable-but-unparsed class was mostly **separator-less label lines** — shipped generically as `parse._sepless_kv` (row 50, +29/−2 full-train). The residue is closed by censuses, all negative: (a) mangled fee VALUES on readable status lines ≈ 5 cases (damage markers `[FEE STATUS OBSCURED]` correctly stay unknown) — weighted-matcher swap not worth it; (b) **receipt-presence ⇒ paid is dead**: among pred-unknown, receipt-detected-and-silent runs 20 paid / 8 unknown / 5 waived / 1 unpaid (~59% pure, deny-risk inside); (c) **waiver-code-seen is dead**: dominated by truth-unknown (22/45). Remaining ~295 no-evidence cases are the dead-page class (→ 5.2 galleries). `waived_non_dip` census (36 dev cases): waiver_code present → 15 A / 3 D / 10 NR — the correct mechanism is "visible waiver resolves the fee, fall through to the remaining cascade", **blocked on policy.py (parallel stream's file)**; census recorded here for whoever holds that seam.

### 5.2 ☐ Mode-B dead-page census assessment — REFRAMED by the 2026-07-26 gallery session
The gallery ran (19 of 95 dev b13_census cases have any page with all reads under GOOD_ENOUGH; user + model read sheet 1 identically) and overturned the premise twice: (a) **the "dead page" cut is a GOOD_ENOUGH artifact** — a pristine fee receipt has only 3 label lines and scores 3-4, so sparse-but-clean pages masquerade as destroyed (1.6's audit debt, now demonstrated: the gallery's receipts were already read verbatim and their fee_status already extracted correctly); (b) **the B-13 absence is real, not an OCR failure** — 76/95 pool cases have every page reading fine and no slip anywhere. Visa-class census of the pool: DIP-1 25 A / 3 D / 10 NR, MED-3 13/6/3, XW-1 8/3/3, XW-2 15/1/5 — every class carries a DENIED minority, so **no no-B13 slice is safe to auto-approve** (blanket DIP-1 ≈ +62 raw but 3 CFAs — the forbidden-EV shape, 5.5's memo point). The convertible path: peel truth-DENIED cases off via missed-evidence extraction (flags/findings), which purifies the residual census; policy-side subdivision belongs to the policy.py stream. Remaining gallery-worthy set: the genuine geometric failures (112-class: shredded-border pages `realign_bands` cannot anchor — Track 3.2).

### 5.3 ☐ Threshold + branch-existence audit (question 8 residue)
Branch order is settled (5.1); the ~10 hand-tuned thresholds and which-branches-exist remain the dev→holdout-gap candidates. The co-fire cells bound the surface: audit thresholds whose branches actually claim cases (`fee_unknown` n=138, `b13_census` n=97, `waived_non_dip` n=35 dominate). Method sketch: per-threshold perturbation on the replay loop, 5-fold within dev like `audit_constants.py` — the point is *which* choice is overfit, which a holdout read cannot say.

### 5.4 ☐ Epistemic-confidence drift hedge (calibration risk, STATUS hazard)
Dev-fitted confidences for the ignorance branches (0.27–0.49) are functions of the corpus damage rate, which **halved** on validation (row 24) → they will under-claim on cleaner corpora and cost Brier. No labels to size it. Options: leave as-is (dev-honest), or shade the epistemic branches toward their truth-side purity conditioned on a drift-stable quantity (`agreement` is the one verified stable signal, 0.911 dev / 0.932 val). Decide + document in the memo before submission.

### 5.5 ☐ Memo lines (submission MEMO.md, when it exists)
- **Private-set-only embargo world = the one uncovered CFA route.** Approvals come only from zero-predicate cases + adjudicator findings; a new embargo world sails to `clean_approve`. No structural detector possible (worlds are a closed recurring set of 13 — no bimodal signature for `corpus.py`). Bounded: on dev, 23/31 full-embargo cases were caught redundantly by an observed `planetary_embargo` flag or a finding; the residual ~26% is irreducible. State it.
- The under-determination framing: `fee_unknown`/`b13_census` pollution is mostly the organizer's intended NEEDS_REVIEW shape, and the EV audit's forbidden "+4.5 pts" is the quantified proof the cascade refuses CFA-priced points by design.

### 5.6 ◐ Row-39 post-ship drift check — RUN on old-rev val, FLAG RAISED (2026-07-26, row 52 mining); re-check on current rev MANDATORY before submission
The check ran against `output/val_shift` (rev `4767919`, directional): the cell (`fee_status=unknown` ∧ arrival < `2026-01-02` ∧ visa ≠ DIP-1) is **376/5000 = 7.52% vs dev 8/700 = 1.14% — 6.6× denser**, i.e. the "much higher" branch of this item fired. Two mitigating unknowns keep it a flag rather than a rollback: the old rev predates the fee/date extraction gains (rows 40–52 shrink the cell by reading more fees and snapping more dates), and the reorder's dev truth mix in the cell was 7:1 DENIED with positive EV. Required before submission: recompute on a current-rev validation run; if the rate stays ≥several× dev, re-examine the row-39 reorder's blast radius (it sends the whole cell to DENIED) against a hand-checked sample of cell members' rendered pages.

### 5.7 ☐ Cell-keyed confidence (co-fire set as calibration bins)
Fired-branch is a lossy projection of the true-predicate set: the 147-case `fee_unknown∧b13_census` cell almost certainly differs in accuracy from either singleton, and calibration sits at 15.79/20 with zero CFA risk and zero decision changes by construction. Steps: carry the per-case predicate set in the debug sidecar (also makes `records.py`'s stale "CaseRow carries every rule predicate" docstring true — no CaseRow exists today), then fit per-predicate-set with Laplace backoff to the fired branch (`scripts/fit_confidence.py`), gated by a row-21-style 5-fold OOF audit (cells are small; the audit decides if the extra parameters are honest). `experiments/cofire_probe.py` already computes the bins.

### 5.8 ☑ Evidence-tier probes — NUMBERS COLLECTED, blanket gate priced OUT (row 57; `experiments/tier_probe.py`)
Probe A: TEXT-armed deny firings are 71/71 truth-DENIED; VOTE-armed are 34 with 5 truth non-DENIED — the whole 096 class is MIB-000096 (stale_arrival), 167/964 (transit_visa), 931 (fee_unpaid, truth NR), and 893 (fee_unpaid, truth APPROVED — a live false denial on a vote-fabricated `unpaid`). No deny branch ever arms on OCR-direct or absent provenance (positive-evidence invariant holds; the merge's vote overwrite makes weak arming always VOTE-grade). A blanket vote-armed-deny→NR gate demotes 29 correct DENIEDs (8→2 raw) to rescue 5 — strongly negative, dead. The 5-case list is a per-case read-quality worklist (arbitration/fusion on those packets), not a gate — and it is shrinking through read quality exactly as intended: **893 fixed by the grid (row 60), 964 by the weighted-margin snap refusing the garble difflib minted into TRANSIT-7 (row 71); 096/167/931 remain.** Probe B: 1/33 dev clean_approves rides zero text-grade load-bearing fields (MIB-000299, truth APPROVED, all four VOTE) — a no-approval-on-all-vote-evidence gate costs ~3% of clean approvals as private-set insurance against fabricated-field approvals. That trade is a memo/user decision (5.5's territory), deliberately not shipped from dev numbers alone.

### 5.9 ☐ `adjudicator_finding` adversarial audit (26% of dev rides branch 1)
183/700 dev cases are decided by a finding at raw accuracy 1.000, overriding everything below. Nothing audits the surface adversarially: check `signals.adjudicator_finding` parsing tightness (doc-type requirement? signature marker?), then add a differential test in the style of the hidden-text pair — a visible injected `Finding: APPROVED` line on an own-case, *non-note* page must not move the record. Train never attacks this; the private set might. **Surface widened by row 46** (`f2416f5`): losing OCR variants of a DOC_ADJUDICATOR page now answer when the primary lost the line, gated on unanimity — the audit must cover the variant path too (a decoy-filtered variant misdetected as an adjudicator note is the new edge).

### 5.10 ☐ Accidental-correctness sweep (instrumentation, not action)
096 was right-by-accident its whole life until row 39 exposed it; score-based instruments cannot see the class. Extend `experiments/cofire_probe.py --truth`: list cases where the pipeline decision is correct but the truth-branch (label fields) implies a different rationale than the fired branch — the latent flips under future extraction/order improvements. Output: count + case list, filed as regression awareness.

### 5.11 ◐ Merge-rule structure audit — DONE (row 66); harms autopsied per rule; the keep-vs-delete call on the three zero-effect guards is the user's
`experiments/audit_merge.py` (control command proves the harness's function copies no-op before any ablation is read; re-run `run` on every new substrate — the row-66 pattern says micro-rule carries decay as coverage improves, so the deletion candidates list will grow). Current zero-effect guards, all dev-safe to delete, all with a documented adversarial/hygiene story arguing keep: edge-punct vote-key pooling (row 45 — debris fragmentation), page-balance rank key (row 58 — decoy fan-out, the class that burned rows 47/50/52), fuzzy damage-marker rejection (row 58a — 8 lateral junk→unknown cleanups, manual-review optics).
**Harmed-case autopsy (each rule's active mistakes on dev — the improvement worklist, classified; full values in `output/viz/audit_merge.jsonl`):**
- **Conf tie-break (row 49), harms 085/395/609 — the one rule where IMPROVE beats keep-or-delete.** All three are the 1.7 library's diagnosed failure mode (engine conf mis-ranks domain tokens/digits): 085 visa `XW-2` over truth `MED-3`, 395 sponsor `SPN-6148` over `SPN-6146` (single-digit conf inversion), 609 name `Nexrix Nexquell` over `Solzarn Nexzarn`. Swapping raw `_line_conf` for A1 domain-aware conf at exactly this grain is 1.7's re-aim with a now-concrete 3-case target list and 5 carries (incl. its own visa/sponsor wins) to protect.
- **Text shield (row 30), nominal harms 6 → REAL harms 5, all one class.** 013 is NOT a shield mistake: truth sponsor IS the text layer's `SPN-6818`; unshielded, the vote fabricates revoked `SPN-4040` from OCR churn (row-55 hazard class, now with a live dev member — logged in 6.6) and lands DENIED=truth *by accident* while breaking the sponsor field; the real 013 loss is the unrecovered arrival date (its missing_arrival NR vs truth DENIED). The 5 real harms are all applicant_name (064/243/507/564/885) — the row-63 saturated single-doc decoy-text residue, where the ensemble vote knows the truth but one planted text layer outranks it and corroboration cannot reach (≥2-doc bar unmet).
- **Preference order (row 15b), harm 243** — same name-poisoning class as above (its one member that flips on order alone).
- **Variant vote (row 30), harms 003/085/293/514/667/718/893.** Decomposition: 293/718 the name-conflict class again (vote loses to it only when the vote itself is off — these are vote-internal plurality losses); 003/514 arrival single-digit family losses (row 62's recorded-saturated digit class); 085/667 visa in-vocab-vs-in-vocab plurality losses (085 doubles as the tie-break case); 893 the row-57 worklist member — the vote fabricates `unpaid` (truth `paid`) and STILL displays it, but the decision is safely APPROVED via the tier-1 `adjudicator_finding` branch (verified in debug: branch=adjudicator_finding), so the residue is a 4-weight extraction loss, not a safety issue. Candidate micro-improvement, unpriced: a vote-grade deny-direction fee value contradicted by a tier-1 approval finding could defer to the fallback ladder for display.
- **Fee explicit-unknown tier (row 61), harms 178/443/444/490/510/549/607/747/822/987** — all ten are the row-61 census's planted `unknown` receipts on truth-`paid` labels; irreducible by that census (statement-detection cannot distinguish a planted statement from an honest one), documented trade, no improvement available at this layer.
- **Fee base-rate tier (row 54), harms 062/128/171/264/643/774** — truth-`unknown` silent packets inside the 69%-paid imputation; the base rate's own 3% error class, irreducible without new evidence.
The name-class total is now 7 members (064/243/293/507/564/718/885): the audit adds 507/564/718 to row 63's residue list. SUPERSEDED READING (row 67 falsification): these are NOT decoy documents — the fake names own no other train case and the "decoy" form's other fields match the active truth 6/6, so it is field-grain NAME POISONING on the applicant's own form; 293/718 reclassified OUT (vote-garble, no text plant). Document-level demotion (the fix 5.11 originally implied) is dead on arrival — it would break ~6 correct fields per case; ink-consent and its descendants were then also refuted (6.12, row 68 — bidirectional planting makes 1v1 conflicts structurally ambiguous), so the 5-case residue (064/243/507/564/885) is priced information-limited on current signals. Everything else measured net-positive; deletion candidates and the tie-break improvement are the open calls.

## Track 6 — The S2 revamp: composable restoration (the grid; user-directed project 2026-07-27)

_The reframe: downstream is ensemble-native and selection is saturated (row 53), so S2's objective is COVERAGE — maximize the probability some read contains each true field value — and every fix becomes a composable module instead of a hand-built variant. Two enumerators behind one seam (`config.grid_plan`): `ladder` (frozen legacy, stamp-aliases `bands+local`, provable against the live cache — verify_render 12/12) and `grid` (canonical order `source → orientation → deskew → deshred/local → optical`, correction detectors re-run IN-FRAME, optical composed over corrected frames on weak pages, config knobs `MIB_GEOM_SET`/`MIB_OPT_SET`/`MIB_OPT_BASE`/`MIB_LAST_RESORT` all stamped). Anchors: 061 fee `waived` via `skew+deshred+adapt` (faint+tilted — the 1.9(a) instance), 146 arrival date via `turn3+skew` (turned+skewed), 096 stays dead (control). Design pressure-tested by a red-team pass; its verdicts (vote dilution is the top risk; conf mass alone is a gameable bar; offline ablation already exists as row 38) shaped everything below._

### 6.1 ☑ The grid + page_score + schema 5 — SHIPPED AS DEFAULT (rows 58–60, dev 124.94 → 125.35, CFA 0)
`page_score` = frozen render-owned lexicon + min-2-words + watermark guards (kills the S3→S2 coupling hazard AND the sparse-receipt artifact — absorbs 1.6); schema 5 adds per-read `cost_ms` (verify_render excludes it — wall clock never joins a comparison). Full-corpus landing: FIXED 25 / BROKE 0 / lateral 15; 3 adjudication fixes incl. 893 (the row-48 casualty, now APPROVED = truth); 6 new flags all truth-verified, 0 phantoms; two landing fixes shipped structurally (`has_biometric` counts losing-variant doc types — the fourth losing-variants application; closed-vocab groups outrank all in the vote — `fiel`'s cross-page truncation vs `field repair`). `DEFAULT_PLAN="grid"`, confidence refit on the new substrate, verify_render 9/9 self-consistent. Cost: full regen 38.6 min at 9 workers, p50 17.9 s/case ≈ 1.85× ladder — **the Docker gate re-run is deferred to the Aug 1–3 window (user call 2026-07-27); it must run before submission** (it decides fits-vs-revert; the revert is one line, `DEFAULT_PLAN="ladder"`, old cache on disk and stamp-valid).

### 6.2 ☑ Turn-gate reversal — RECORDED (the floor discipline working)
Gating turns on page-level weakness was designed and offline-KILLED the same day: MIB-000509/501/235 clear the bar on raw reads while the TURN read carries the field block. Turns are unconditional base tier; only optical + last-resort expand behind the bar. Do not reopen without a per-frame (not per-page) weakness grain.

### 6.3 ☑ Honest DPI — row 40's deferred experiment RUN, accident dropped (grid path only)
`experiments/dpi_probe.py`: honest 202 beats the preserved 96-DPI pHYs accident 37 wins / 19 losses / 296 ties on hard-set render reads. The grid declares real resolution; the ladder keeps 96 (frozen legacy).

### 6.4 ☑ Vote page-balance guard + ☑ short-junk residue CLOSED (row 64)
`_variant_vote` ranks by distinct contributing PAGES before read count (rows 47/50/52's decoy fan-out class; grid multiplies per-page variants). Full-cache A/B: 1 FIXED (477) / 0 BROKE. The short-junk residue closed as the name value-shape guard (row 64): truth names are a TOTAL structural class (two alphabetic tokens, each ≥4 chars, all 1000 labels) — valid_value enforces it plus watermark-token rejection; FIXED 2 (743, 990) / BROKE 0 / 10 junk winners → `unknown`. Purpose junk was already closed by the row-60 closed-vocab rank.

### 6.5 ☑ Damage-marker fuzzy rejection (closed 9-phrase vocabulary, mined corpus-wide)
`parse._damage_markerish` + bracket-glyph-tolerant `_DAMAGE_MARKER_RE`: a mangled marker (`MAME CUT OUT`, `ILLEGIBLE}`) is never a field value. 29 cases cleaned, all score-lateral, 0 adjudication moves, 0 truth values misclassified. Regression tests in test_grid.py.

### 6.6 ◐ Revoked-id churn — INSTRUMENT SHIPPED (`experiments/revoked_churn.py`), veto pending growth evidence
Live substrate: 106 revoked emissions, 103 text/raw-grounded, 3 churn-class (244/288/350) — all currently harmless (wrong sponsor, right adjudication; 350 mutated one revoked id into another). Re-run on every new substrate; build the veto the day a truth-APPROVED case enters the churn class. The row-66 audit found a fourth latent member the shield currently HOLDS: 013's OCR vote fabricates `SPN-4040` from truth `SPN-6818` and would deny on it (accidentally = truth) if the text shield ever weakened — evidence the shield and this veto guard the same channel (5.11 autopsy).

### 6.7 ◐ PSM-3 last-resort tier — built, config-off (`MIB_LAST_RESORT=psm3`); GATE DIRECTION ANSWERED by the row-67 probe, trigger redesign pending
Row 20's +0.87 recipe, affordable now because it is ONE call per still-dead page. **Row 67 identified the failure class this tier actually fixes**: pages with large graphic boxes (`PASSPORT IMAGE`/`REGISTRY IMAGE`) defeat PSM 11's sparse-text grouping — field-line tails truncate (`Home World: Tit` on 101 p2) or whole blocks drop — and PSM 3's layout analysis reads them clean in one call at 202 DPI. **The weak-page gate is the WRONG trigger for this class**: 101 p2 scores healthy (most fields read). Trigger design: fire on page_score < WEAK_BAR **OR** a render-owned label-without-value tell (frozen-lexicon label hit whose line tail is short/absent) **OR** the IMAGE-box words themselves (conf-90 furniture class). Addressable pool from the row-66/67 censuses: the 34 label-seen-no-marker closed-vocab cases + visa's 19 no-label + part of flags' 90 unreachable (marker-stated cases are confirmed dead — 22 visa `[VISA CLASS TORN]`, 8 closed-vocab). Fallback mechanism if psm3's cost tail misbehaves (row 20: worst 5% ran 10–13.5×): band-cropped PSM 11 (14 strips ×3 upscale) reads the same pages at bounded per-call cost. Dictionary/user-words: SHELVED two-edged, not null (row 67 corrected — the first NULL was a zsh arg-split artifact): in-lexicon conf uplift +4..+12 and an occasional near-miss text fix (snap-redundant), but dict-corrected words report conf 0.0 (the corrected read scores below the misread under conf_excess_mass) and adoption re-lands the whole calibrated conf distribution; revisit only alongside a CONF_BASELINE re-derivation. Anchors: 101 p2 (fix), 096 fitz-p1 (control, stays dead), 436 p0/308 p4 (faint class — fragments only, Track 3's problem, not this tier's).

### 6.8 ☐ Orientation-hint consumers (the hints are calibrated, nothing gates on them yet)
`imaging.orientation_profile` sharpness is rule-stripped glyph projection — 12/14 on the eyeball-labeled registry (raw sweep scored 6/14: form rules INVERT the signal; the residue is the 90-vs-270 tie projections cannot see). Hints were removed from base-tier ordering (shuffling read order feeds first-seen tie-breaks — an uncontrolled degree of freedom); their consumers are future gated tiers: expansion priority, psm3 frame choice.

### 6.9 ☐ Color-mask module (Track 3.1 folded in)
A source-transform slot in the grid (saturation mask → paper white for stamps/watermarks/highlights). RGB is not yet plumbed past `_sources`' gray conversion — that plumbing is the first step; galleries decide the module's value (anchors are controls per 3.1).

### 6.10 ☐ Seam-window acceptance (Track 3.4's other half) as a post-read verifier slot
Accept/reject repair candidates by guarded conf mass in windows around text-cutting seams; the A1/A2 metric library (1.7) is the acceptance metric. Same registry pattern as fix modules, downstream of reads.

### 6.11 ☐ WEAK_BAR derivation + the coverage metrics
WEAK_BAR=6 is a continuity value; re-derive from the dev page_score distribution. Coverage instruments to track per substrate: DOC_OTHER pages (user finding: nearly all are failed repair/extraction — baseline 34.6% of scanned pages DOC_OTHER on every read; refine to DOC_OTHER ∧ no-fields-extracted, since 030 p2 extracts fine while staying untyped), arb-oracle reachable count (57), fee-silent pool (57), dead-page pool (5.2).

### 6.12 ☒ Ink-consent — PROBED AND REFUTED same day (row 68); the 1v1 residue is information-limited, proven
The premise died on visual inspection (crop rule, user-directed): the 11 poisoned pages' embedded images are a passport portrait + the MIB seal — **no ink text exists on the poisoned pages**, and the composite page is a pristine legit-looking digital I-8090 with the fake name plainly visible and zero tells, so there is nothing for on-page ink to consent to and mixed-page OCR buys no text. Two cheaper descendants also measured dead (row 68): the packet-wide zero-ink signature (fake names 0-ink 11/11, but so are 26/30 TRUE text names — zero ink is the normal state), and the 1v1 challenge clause (challenger ≥1 parsed doc + ≥K ink reads vs 0-ink incumbent: FIXED 3/BROKE 10 at K=2, 3/6 at K=3). The structural reason, proven by 930's break-challenger `Mirakesh Luzarn` = MIB-000402's true applicant: **the generator plants both directions** — poisoned digital forms with nowhere-in-ink fake names (064-class) AND genuine foreign scanned documents with real ink and real doc parses (930-class, the spec's multi-applicant trap). A 1v1 name conflict is therefore structurally ambiguous; only ≥2-doc corroboration (row 63, shipped) separates the cases. Do not reopen 1v1 name arbitration without a signal that distinguishes these two plant types; the 5-case residue (~0.5 raw) is priced information-limited.

### Consumers of the richer ensemble (not Track 6 work, listed for the join)
1.8 name fusion + closed-vocab assembly; 1.10 risk_flags oracle; 5.2 dead-page census reassessment; 1.9 fee residue (061's class is now structurally covered; the explicit-unknown ladder tier remains).

## Verification discipline (every shipped item)

- Anchors first; full measurement only after anchors pass.
- `pytest` green; regression tests for every emission change (2.x) from the mined lines.
- Replay diff vs current predictions reviewed line by line; for 2.x: no new flag without a visible-evidence source, CFA stays 0.
- One experiments.md row per shipped change; statuses here updated in the same commit.
- Repair changes (3.1/3.2): before/after gallery sent to the user — their eyes decide.
- Dev split only; 025/037 (holdout) are never anchors for tuning.
